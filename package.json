{
  "name": "{{ name }}",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "{{ author }}",
  "license": "ISC",
  "dependencies": {},
  "main": "index.js",
  "devDependencies": {},
  "description": "{{ description }}"
}

